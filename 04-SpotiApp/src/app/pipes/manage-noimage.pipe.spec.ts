import { ManageNoimagePipe } from './manage-noimage.pipe';

describe('ManageNoimagePipe', () => {
  it('create an instance', () => {
    const pipe = new ManageNoimagePipe();
    expect(pipe).toBeTruthy();
  });
});
